# goOS

This operating system is bare bones. It boots and it types--that is it.

You can boot the image using `qemu-system-i386` on the `boot.iso` file.

```
	qemu-system-i386 -drive format=raw,file=boot.iso
```

![goOS](res/demo.png)
